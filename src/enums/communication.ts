export enum CommunicationType {
  EMAIL = "EMAIL",
  CALL = "CALL",
  MESSAGE = "MESSAGE",
}

export enum CommunicationStatus {
  READ = "READ",
  UNREAD = "UNREAD",
  ARCHIVED = "ARCHIVED",
}

export enum CommunicationDirection {
  INBOUND = "INBOUND",
  OUTBOUND = "OUTBOUND",
}
